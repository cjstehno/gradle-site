plugins {
    id "org.ajoberstar.github-pages" version "1.5.1"
}

githubPages {
    repoUri = 'git@github.com:cjstehno/gradle-site.git'
    pages {
        from(file('build/site')) {
            into '.'
        }
    }
}
