plugins {
    id "org.ajoberstar.github-pages" version "1.6.0"
}

githubPages {
    repoUri = 'git@github.com:cjstehno/gradle-site'
    pages {
        from(file('build/site')) {
            into '.'
        }
    }
}
